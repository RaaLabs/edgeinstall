#clear-linux-config
#generated by bt
#generated on: 2020-02-24_12:05:24_UTC
targetMedia:
  - name: ${release}
    size: "4000000000"
    ro: "false"
    rm: "false"
    type: disk
    children:
    - name: ${release}1
      fstype: vfat
      mountpoint: /boot
      label: boot
      size: "150000000"
      ro: "false"
      rm: "false"
      type: part
    - name: ${release}2
      fstype: swap
      label: swap
      size: "256000000"
      ro: "false"
      rm: "false"
      type: part
    - name: ${release}3
      fstype: ext4
      mountpoint: /
      label: root
      size: "3594000000"
      ro: "false"
      rm: "false"
      type: part
keyboard: us
language: en_US.UTF-8

users:
  #- login: raalabs
  #  username: Clear Linux OS
  #  admin: true
  #  # to generate a password
  #  # clr-installer --genpass <password>
  #  password: $6$tSfH35cpQ2StgBXQ$A4PMcdCkJD3ruf.IruL4h9jgkcMn1A/KSfR8rDi0aFyqVq4Afmx3lca3QICnYZlqcqaFRr5c9bkbK7zqNdigP/
  - login: dolittle
    username: Dolittle Default User
    admin: true
    password: $6$UmdNOTE5nqWV/2HX$FGyFdMk6gKyV2xImJIylHxnDZ2Dc2rPtukuvclNjKc/yGG0AeafshywvI7RlzuaHemIXQAB6Ar9Ufr.cVVZ4X0

bundles: [aspell-fr, strace, sysadmin-basic, cloud-native-basic, os-core-plus, procps-ng,
    wpa_supplicant, xz, aspell-de, gzip, linux-firmware-extras, openvswitch, os-core,
    acpica-unix2, dolittle-edge, powertop, python3-basic, tmux, glibc-locale, minicom,
    socat, NetworkManager, bootloader, dpdk, file, libstdcpp, parallel, tzdata, znc,
    git, iproute2, kbd, lib-openssl, linux-firmware, linux-firmware-wifi, os-core-update,
    diffutils, iptables, jq, less, net-tools, openssl, aspell, clr-installer, clr-network-troubleshooter,
    ethtool, openssh-server, zstd, aspell-es, containers-basic, gnupg, libglib, man-pages,
    rsync, unzip, vim, curl, p11-kit, perl-basic, shipos-branding, sudo, dolittle-edge-dell-5000,
    findutils, htop, os-core-webproxy, syslinux, azure-iotedge, openssh-client, wget,
    iperf, network-basic, patch, which, kernel-lts, kernel-native, kernel-install, shells, ModemManager]
timezone: UTC
kernel: kernel-native
postArchive: true
autoUpdate: true
block-devices: [{name: release, file: release.img}]
iso: true
keepImage: true

# Copy the <mixfolder>/script folder into the installer image.
# The scripts folder should contain the .yaml file to be used
# when running clr-install --config <config.yaml>.
# It should also contain whatever postinstall script is defined
# in the yaml file in the same directory.
post-install: [
  post-install: [
    {cmd: "cp -a ${yamlDir}/scripts ${chrootDir}/root/"},
  ]
]